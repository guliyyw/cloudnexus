server:
  port: 8081
  host: "0.0.0.0"

database:
  dsn: "host=postgres user=cloudnexus password=cloudnexus dbname=cloudnexus port=5432 sslmode=disable"

redis:
  addr: "redis:6379"
  password: "cloudnexus"
  db: 0

minio:
  endpoint: "minio:9000"
  access_key: "minioadmin"
  secret_key: "cloudnexus"
  use_ssl: false
  bucket: "cloudnexus"

log:
  level: "info"
  format: "json"

jwt:
  access_secret: "cloudnexus-dev-jwt-access-secret-key"
  refresh_secret: "cloudnexus-dev-jwt-refresh-secret-key"
  access_ttl_sec: 28800
  refresh_ttl_sec: 604800
